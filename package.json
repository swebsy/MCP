{
  "name": "@swebsy/mcp",
  "version": "0.8.0",
  "type": "module",
  "description": "MCP server that lets a coding agent (Claude Code, Codex, Cursor, Windsurf, …) drive an open Swebsy Studio tab over a local 127.0.0.1 WebSocket relay.",
  "license": "MIT",
  "author": "Swebsy",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/swebsy/MCP.git"
  },
  "mcpName": "io.github.swebsy/mcp",
  "homepage": "https://docs.swebsy.com/settings/ai-coding-agent/",
  "keywords": [
    "mcp",
    "modelcontextprotocol",
    "swebsy",
    "website-builder",
    "claude-code",
    "coding-agent"
  ],
  "bin": {
    "swebsy-mcp": "./dist/server.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "esbuild src/server.ts --bundle --platform=node --format=esm --target=node20 --outfile=dist/server.js --banner:js=\"import { createRequire } from 'node:module'; const require = createRequire(import.meta.url);\"",
    "prepublishOnly": "pnpm build"
  },
  "devDependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@swebsy/ai-tool-contracts": "file:./ai-tool-contracts",
    "@types/node": "^25.9.5",
    "@types/ws": "^8.18.1",
    "esbuild": "^0.28.2",
    "typescript": "^5.9.3",
    "vitest": "^3.2.7",
    "ws": "^8.21.3"
  }
}
